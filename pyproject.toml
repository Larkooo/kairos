[project]
name = "temporal-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "huggingface-hub>=1.10.1",
    "safetensors>=0.7.0",
    "torch>=2.11.0",
    "transformers>=5.5.3",
]
